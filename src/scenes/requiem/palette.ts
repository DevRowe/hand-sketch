/**
 * The Requiem etching palette: bone paper, one black, and two colours wiped into the plate a la poupee.
 * The film's frames are black grounds with a single lit subject; here the black is bitten tone and the light is the
 * paper left clean, so the only colours are a warm and a cold accent, never both at full strength in one place.
 */
export const REQUIEM = {
  /** Bone: a warm, faintly grey etching stock. */
  paper: '#e9e0ca',
  /** Lamp black with a trace of umber: every line, hatch, grain and solid. */
  ink: '#15100c',
  /** Sanguine: flame, red hair, amber glass, blood. */
  warm: '#b8431d',
  /** Prussian blue: iris, screen light, cells, cold glass and water. */
  cold: '#255d7e',
} as const;
